# Direct dependencies for bbva-bank-statement-pdf-to-excel.
# Extraction parses the PDF text layer (pdfplumber); the Excel writer uses openpyxl;
# requests talks to a local Ollama server for optional categorization.
# pdfplumber pulls in pdfminer.six / pillow / charset-normalizer transitively.

pdfplumber==0.11.10
openpyxl==3.1.5
requests==2.34.2

# Dev / test only.
pytest==9.1.1
